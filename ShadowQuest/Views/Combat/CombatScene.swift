// CombatScene.swift
// ShadowQuest
//
// Scene SpriteKit pour le rendu visuel des combats

import SpriteKit

class CombatScene: SKScene {

    // TODO: Phase 3 — Implementer la scene de combat SpriteKit
    // Sprites joueur/monstre, animations d'attaque,
    // effets visuels (degats, soins, buffs), background

    override func didMove(to view: SKView) {
        backgroundColor = .black
    }
}
