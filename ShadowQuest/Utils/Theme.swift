// Theme.swift
// ShadowQuest
//
// Theme visuel — couleurs, polices, dimensions

import SwiftUI

enum Theme {
    // TODO: Phase 1 — Definir le theme dark fantasy

    // Couleurs principales
    static let background = Color.black
    static let primary = Color(red: 0.8, green: 0.2, blue: 0.2)    // Rouge sang
    static let secondary = Color(red: 0.6, green: 0.5, blue: 0.8)  // Violet mystique
    static let accent = Color(red: 0.9, green: 0.7, blue: 0.2)     // Or
    static let textPrimary = Color.white
    static let textSecondary = Color.gray
}
